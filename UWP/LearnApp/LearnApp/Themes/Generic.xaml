<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LearnApp"
    xmlns:local2="using:LearnApp.Excercise34">

    <Style TargetType="local2:MyTemplatedControl" xmlns:local2="using:LearnApp.Excercise34">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:MyTemplatedControl">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
